<%= turbo_frame_tag "flash-notifications", class: "z-50 space-y-4 fixed top-0 max-w-sm w-full" do %>
  <% flash.each do |type, msg| %>
    <%= render NotificationComponent.new(type: type, data: msg) %>
  <% end %>
<% end %>
